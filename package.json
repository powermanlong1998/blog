{
  "name": "my-hugo-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.160.0/hugo_extended_0.160.0_linux-amd64.tar.gz | tar -xz && chmod +x hugo && ./hugo --minify"
  }
}
